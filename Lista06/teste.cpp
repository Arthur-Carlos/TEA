#include <iostream>
#include <string>

#define MAX 300

using namespace std;

int main()
{
    int entrada, N[MAX], i, prim, seg;
    double matriz[MAX][MAX];
    for (i = 0; i < entrada; i++)
    {
        for (int j = 0; j < entrada; j++)
        {
            cin >> matriz[i][j];
        }
    }
    for (i = 0; i < entrada; i++)
    {
        cin >> N[i];
    }
    for (i = 0; i < entrada; i++)
    {
        cin >> prim >> seg;
        
    }
    
}